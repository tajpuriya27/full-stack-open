import { createSlice } from "@reduxjs/toolkit";

const notifyReducer = createSlice({
  name: "notifiy",
  initialState: "",
  reducers: {},
});

export const {} = notifyReducer.actions;
export default notifyReducer.reducer;
